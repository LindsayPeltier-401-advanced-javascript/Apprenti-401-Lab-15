'use strict';

// const { server } = require('../src/app');
// const supergoose = require('@code-fellows/supergoose');
//const mockRequest = supergoose(server);

describe('proof of test', () => {
  it('will pass heroku', () => {
    expect(true).toEqual(true);
  });
});
